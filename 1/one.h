#ifndef ONE_H
#define ONE_H
struct Soc {
    char emails[50]; //  max 50 characters each
    char social_media[50]; //  max 50 characters each
    char messengers[50]; //  50 characters each
};
#endif
